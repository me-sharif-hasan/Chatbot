# Application Configuration
app.name=GitFront
app.version=1.0.0
app.server.port=8080

# Database settings
db.host=localhost
db.port=5432
db.name=mydatabase
db.user=admin
db.password=secret

feature.x.enabled=true
feature.y.enabled=false
